import { Commit } from '../../src/changelog/types'

describe('Issues - Transformer', () => {
  describe('FetchPullRequestNumbers', () => {
    test('it should remove all duplicates from a changelog', (done) => {
      done()
    })
  })
})
